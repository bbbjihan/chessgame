import styled from "styled-components";
import { alignCenter, size } from "../styles/macros";

export const HeaderWrap = styled.div`
  color: white;
  font-weight: bold;
  font-size: 1.5rem;
  ${size(`100%`,`100%`)}
  ${alignCenter()}
`